spring.application.name=onboarding-agent
# ? FIX #1: Never hardcode API keys in Java files.
# Store secrets here or in environment variables.
# In production, use environment variables or a secrets manager.
my.api.key=${MY_API_KEY:dummy-key}
server.port=${PORT:8080}
